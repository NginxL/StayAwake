# 致谢

本项目在功能理念上参考了 [KeepingYouAwake](https://github.com/newmarcel/KeepingYouAwake)
（Marcel Dierkes，MIT License）：使用 macOS 自带的 `caffeinate` 为菜单栏防休眠工具提供底层能力。

StayAwake 的 Swift / SwiftUI 实现、中文面板和图标为独立编写，未复制 KeepingYouAwake 的源码或图片资源，
不是该项目的官方版本或关联产品。

菜单栏与界面使用系统 SF Symbols；应用图标由 `scripts/make-icon.swift` 绘制。
